;;; init-toml --- Nothing
;;; Commentary:
;; toml is also an object of GitHub.
;;; Code:

;; 好像好久没开发了.
(require 'toml-mode)
(add-hook 'toml-mode-hook 'goto-address-prog-mode)


(provide 'init-toml)
;;; init-toml ends here
